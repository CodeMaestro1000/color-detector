This software is being built to identify colors from images. The user simply uploads a photo and then uses a rectangle to
select the ROI and then the system generates all the colours found in that region of interest along with rectangles
showing the colors and their rgb, hex values.